optimizer:
  _target_: torch.optim.AdamW
  lr: 1e-4
lr_scheduler:
  _target_: torch.optim.lr_scheduler.ReduceLROnPlateau

vae:
  ckpt_path: /groups/gag51394/users/asai/dacvae.pt
  hidden_size: 64

mmdit:
  hidden_size: ${model.vae.hidden_size}
  max_seq_len: 2000
  depth: 12
  heads: 12

sample_rate: ${data.datamodule.sample_rate}
