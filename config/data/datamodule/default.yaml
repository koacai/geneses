train:
  dataset_path: ${data.preprocess.webdataset_dir}/train/data-{000000..000077}.tar
  batch_size: 16
valid:
  dataset_path: ${data.preprocess.webdataset_dir}/valid/data-{000000..000091}.tar
  batch_size: 4
num_workers: 4
use_ddp: false

max_duration: ${data.preprocess.duration}

sample_rate: ${data.preprocess.mimi.sr}
