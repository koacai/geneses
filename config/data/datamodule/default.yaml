train:
  dataset_path: ${data.preprocess.webdataset_dir}/train/data-{000000..000046}.tar
  batch_size: 8
valid:
  dataset_path: ${data.preprocess.webdataset_dir}/valid/data-{000000..000056}.tar
  batch_size: 4
num_workers: 16
use_ddp: false

max_duration: ${data.preprocess.duration}
hubert_model_name: sarulab-speech/hubert-base-jtube
