train:
  dataset_path: ${data.preprocess.cfg.webdataset_dir}/train/data-{000000..000260}.tar
  batch_size: 64
valid:
  dataset_path: ${data.preprocess.cfg.webdataset_dir}/valid/data-{000000..000074}.tar
  batch_size: 4
num_workers: 16
use_ddp: false

max_duration: ${data.preprocess.cfg.duration}

sample_rate: ${data.preprocess.cfg.mimi.sr}

stats_path: ${data.preprocess.cfg.stats_path}
