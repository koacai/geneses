train:
  dataset_path: ${data.preprocess.cfg.webdataset_dir}/train/data-{000000..000089}.tar
  batch_size: 16
valid:
  dataset_path: ${data.preprocess.cfg.webdataset_dir}/valid/data-{000000..000069}.tar
  batch_size: 4
num_workers: 8
use_ddp: true

ssl_model:
  name: facebook/w2v-bert-2.0
  sample_rate: 16000
  layer: -1
  add_adapter: true

vae:
  max_duration: 20
  sample_rate: 24000
