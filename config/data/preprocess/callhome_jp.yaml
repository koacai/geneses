_target_: dialogue_separator.data.preprocessor_conversation.PreprocessorConversation
cfg:
  shar_dir: ${data.dataset.shar_dir}
  webdataset_dir: /groups/gag51394/users/asai/webdataset/dialogue-separator/callhome_jp
  stats_path: ${data.preprocess.cfg.webdataset_dir}/stats.json
  device: cuda

  duration: 30
  train_ratio: 0.95
  shard_size:
    train: 2e8
    valid: 1e7

  mel:
    n_fft: 1024
    n_mels: 80
    sample_rate: 22050
    hop_length: 256
    win_length: 1024
    f_min: 0
    f_max: 8000
